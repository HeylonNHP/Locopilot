# ── Server ──────────────────────────────────────────────────────
# Preferred port for the Locopilot web server.
# If unset, defaults to 3000.
# If the port is busy, the next available port is used automatically.
PORT=3000
